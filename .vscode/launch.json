{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach QEMU",
            "type": "gdb",
            "request": "attach",
            "cwd": "${workspaceRoot}",
            "executable": "bin/kernel.elf",
            "remote": true,
            "target": ":1234",
            "valuesFormatting": "prettyPrinters"
        }
    ]
}